# cs_lab_adv — Copilot Studio 중급 교안

**단일 책임: 중급(2일차) 과정을 "어떻게 보여줄지".** 랩 문서·사이트 구조·렌더링을 소유한다.
Jekyll(just-the-docs) 사이트다. HR 채용 자동화를 소재로 커넥터·에이전트 흐름·승인·토픽을 다룬다.

**소유하지 않는 것**
- 입문(1일차) 과정 → 별도 저장소 [cs_lab](https://jm-2ktech.github.io/cs_lab/)
- GHCP 트랙 → 별도 저장소 `cs_lab_ghcp`
- 교안 **소재 원본**(시나리오·환경 스펙·지식소스 실물) — 이 저장소 밖이다
- 회차 시간표·운영 편성 → 강사 자산

---

## 지금 이 저장소에서 하는 일

**스크린샷 재촬영 + 본문 교정.** 2026-08-31~09-02 에 교안대로 실제 환경에서 랩을 따라 해 보고
본문과 화면이 어긋난 지점을 모아 두었다. 그걸 반영하는 작업이다.

| 문서 | 무엇 |
|---|---|
| [_instructions/실측_리포트_2026-09.md](_instructions/실측_리포트_2026-09.md) | **무엇을** 고쳐야 하는가 — 랩별 실측 결과 |
| [_instructions/작업_지침.md](_instructions/작업_지침.md) | **어떻게** 고치는가 — 단계 번호·스크린샷·문체·검증 |

작업을 시작하기 전에 **둘 다 읽는다.** 특히 「단계 번호」와 「손으로 확인할 것」 절.

스크린샷은 **`/capture` 스킬**이 한 장씩 안내한다 — 어떤 화면을 어떤 상태로 띄울지 일러 주고,
찍힌 파일을 열어 맞는 화면인지 검사한 뒤 「촬영:」 표시를 지운다.
브라우저 자동화로 대신 찍지 않는다(스케일이 안 맞고 MFA 로그인이 막힌다).

---

## 검증 — 커밋 전에 반드시

```bash
bundle exec jekyll build
python _instructions/linkcheck.py     # ADVANCED·OTHER·step numbering 이 셋 다 0
python _instructions/stylecheck.py    # 현재 62점(C). 떨어뜨리지 않는다
```

`linkcheck.py` 가 잡는 것 중 제일 자주 깨지는 건 **`{: start="N" }` 단계 번호**다.
단계를 하나 넣거나 빼면 뒤따르는 모든 `###` 섹션의 `N` 이 밀린다. 상세는 작업 지침 §2.

---

## 함께 봐야 하는 것들

**`docs/lab4.md` 는 2026-09-07 부터 비활성이다. `docs/lab4-alt.md` 가 Lab 4 본편이다.**
차기 고객사가 Outlook 을 쓰지 않아 메일 트리거 판을 학생 동선에서 뺐다. **일시적이다 — 되돌릴 것을 전제로 한다.**

| 무엇을 했나 | 어디 |
|---|---|
| `nav_exclude` · `search_exclude` | `lab4.md` frontmatter — 좌측 메뉴와 상단 검색에서만 사라진다 |
| `nav_order` 5 로 승격 · title 에서 「-대안」 제거 | `lab4-alt.md` frontmatter |
| Lab 4 링크를 `lab4-alt.html` 로 · 「↳ 대안」 행과 Outlook 안내 콜아웃 삭제 | `index.md` |
| 2부 합계 2시간 50분 → **2시간 40분**, 총 220분 → **210분** | `index.md` · `README.md` |

**파일도 URL 도 살아 있다.** `docs/lab4.html` 로 직접 들어가면 그대로 보인다 — 강사용 경로다.
되살리는 절차와 종전 값은 두 파일의 저작 메모 첫머리에 적어 두었다.

**남은 것** — `lab4-alt.md` 본문에 「원본 Lab 4」·「대안」 표현이 넷 남아 있다. 숨긴 랩을 가리키는 말이라
전환이 확정되면 같이 손본다. 지금 고치면 되돌릴 때 두 번 일한다.

**`docs/lab4.md` 와 `docs/lab4-alt.md` 는 짝이다.** 본문 블록 20개를 공유한다 — 한쪽만 고치면 어긋난다.
게다가 **정본이 뒤집힐 예정이다**(차기 고객사가 Outlook 미사용 → alt 가 본문이 된다).

**시간 숫자는 네 곳에 물려 있다** — 각 랩 머리의 「예상 시간」과 `{: .time }` 타이머,
[index.md](index.md) 의 표와 두 합계, [README.md](README.md) 의 총 분수,
그리고 `assets/download/` 의 오리엔테이션 pptx. **pptx 는 저장소에서 못 고친다** — 현재 어긋나 있다.

**`nav_order` 가 왼쪽 메뉴 순서다.** 부록을 추가하면 뒤따르는 것들을 민다.
현재 Lab 7 = 9, 부록 = 10·11, 용어집 = 12. 부록 2개 추가안은 실측 리포트 §부록 후보에.

**저작 메모(`<!-- 저작 메모(학생 비노출): … -->`)에 결정과 이유가 쌓인다.**
본문을 고치면 그 아래에 날짜와 함께 한 줄 남긴다. 다음 사람이 되돌리지 않게 하는 장치다.

---

## 공유 인프라 — 정본은 `cs_lab` 이다

아래 파일은 교안 저장소 셋이 공유한다. **정본은 `cs_lab` 이고 여기 것은 사본이다.**

| 파일 | 무엇 |
|---|---|
| `_includes/head_custom.html` | **한글 검색 고침**(lunr trimmer). 없으면 상단 검색이 한글 0건이 된다 |
| `_includes/nav_footer_custom.html` | 콜로폰 |
| `_sass/custom/custom.scss` | 콜아웃·「확인」 절 `.checklist`·복사 버튼 스타일 + **`ol[start]` 존중** |
| `_instructions/linkcheck.py` · `stylecheck.py` | 검증 |
| `Gemfile` · `Gemfile.lock` | 테마 고정 |

**여기서 고치지 않는다.** 고칠 일이 생기면 `cs_lab` 쪽에 반영하고 내려받는다.
`README.md` 가 안내하는 `python ../_tools/sync_infra.py` 가 그 대조 도구인데,
**이 저장소만 클론했다면 그 스크립트가 없다** — 그때는 건너뛴다.

이 규칙이 있는 이유 — `cs_lab` 에서 `custom.scss` 를 정본·공개본으로 갈라 뒀다가
새로 넣은 CSS 가 공개본에 조용히 안 간 사고가 있었다. **코드 파일을 갈라 두면 그 파일에 생기는 변경이 조용히 누락된다.**

`_config.yml` 은 공유 대상이 아니다 — `title`·`description`·`baseurl` 이 저장소마다 다르다.
다만 `callouts`·`search`·`defaults` 블록은 같은 값이어야 하므로 한쪽을 고치면 같이 본다.

---

## 이 저장소의 유래

2026-08-27 에 `cs_lab` 에서 분리했다. 그전까지 입문·중급이 한 저장소에 있었고
중급은 `docs/advanced/` · `assets/advanced/` subtree 로 들어가 있었다.

`git filter-repo` 로 이식했으므로 **init 커밋부터의 이력이 남아 있다.**
다만 실질 내용은 init 커밋에 통째로 들어왔고 이후는 사소한 수정이다.

**분리하며 바꾼 것 — 이전 이력을 읽을 때 감안할 것**

- `docs/advanced/` → `docs/`, `assets/advanced/` → `assets/` 로 한 단계 끌어올렸다
- 자식 문서 12개에서 `parent: 중급 — HR 채용 자동화` 를 뺐다
- `docs/index.md` 를 루트 `index.md` 로 올렸다(`nav_order: 0`)
- 입문을 가리키던 링크 3개를 절대주소로 바꿨다
- 「입문 Lab 1~8」이 사실 오류여서 「입문 Lab 1~6」으로 고쳤다

**`중급 Lab N.` 접두사는 그대로 두었다.** 원래 이유(입문 Lab 1~6 과 번호가 겹쳐 구분자가 필요했다)는
저장소가 갈리며 사라졌지만, 떼는 것은 제작자 판단 영역이라 손대지 않았다.
뗄 때는 13개 문서의 `title` 을 한 번에 본다.

---

## 미해결 과제

- **「확인」 절 체크박스 43개 미정리.** 입문은 원문자(①②③) + `{: .checklist }` 로 전환했고
  중급은 `- [ ]` 그대로다. kramdown 이 이걸 `<input type="checkbox" disabled>` 로 렌더해
  **화면에 체크박스가 보이는데 눌리지 않는다**
- **`stylecheck.py` 62점(C).** 감점의 99% 가 **볼드 과다**다 — 한 문단에 볼드는 하나까지
- **인터미션 덱 미착수.** 축은 **HITL**(사람이 어디서 끊는가)이고 두 종류가 나온다 —
  비동기(Lab 5, 이벤트가 사람을 부른다) / 대화형(Lab 7, 사람이 먼저 말을 건다).
  중급은 콜아웃이 이미 얇아(스텝당 0.32~0.71) 본문 다이어트가 아니라 **랩 소개·리뷰**가 덱의 목적이 된다.
  Lab 7(58스텝·50분)은 다른 랩의 두 배쯤, 나머지 여섯은 8~10장. 액센트는 보라(`--mauve`)로 잡아 두었다(확정 아님)
- **`중급 Lab N.` 접두사 존치 여부**
- 나머지는 [_instructions/실측_리포트_2026-09.md](_instructions/실측_리포트_2026-09.md) 에
