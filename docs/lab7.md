---
title: 중급 Lab 7. 전형단계 변경 — 토픽
nav_order: 9
---

# Lab 7. 전형단계 변경 — 토픽 (대화형 HITL) 💬

> **이번 랩 완성물**: 지원자 조회 → 두 겹 게이트 → 확인 카드로 전형단계를 변경하는 토픽 + 전용 흐름
> **예상 시간**: 60분 · **완성 신호**: "○○○ 서류합격 처리해줘" → 확인 카드 → 전형단계 변경

<!-- 저작 메모(학생 비노출):
     - 전형단계 유효 값: 검토중/서류합격/불합격 (최종합격/면접합격은 Choice에 없음 — 지침 블록 필수)
     - ApproveStatus = 커스텀 컬럼(Choice), Value = "승인됨" (CJ 환경. SP moderation 아님)
     - 카드: TextBlock 단일 타입 수식 카드(FactSet 혼용 시 null bleeding → AdaptiveCardInvalid)
     - ID 조건부 필터: 세부정보→입력에서 AI적합도+ID 모두 추가. 동명이인은 오케스트레이터 front + 토픽 count=1 back 이중 방어
     - 모든 토픽 종료 필수 — 없으면 재귀 호출 발생
     - 흐름 먼저 빌드 → 토픽에서 바로 연결 (순서 바꾸면 흐름 호출 노드에서 선택 불가)
     ★ 단계 번호는 두 가지가 함께 있어야 맞는다 (2026-07-28 발견·수정):
       (1) 각 ### 섹션 첫 단계 바로 위의 `{: start="N" }` IAL — 없으면 kramdown이 소스 번호를 버리고 시작 번호 없는 목록만 낸다.
           ### 헤딩마다 목록이 끊기므로 섹션마다 필요하다. 단계 추가·삭제 시 N도 같이 갱신할 것.
       (2) _sass/custom/custom.scss 의 "순서 목록 — start 속성 존중" 블록
           — 테마가 CSS 카운터로 번호를 직접 그려 start 속성을 무시하므로, 그걸 꺼야 (1)이 화면에 반영된다.
       둘 중 하나만 있으면 화면 번호가 1부터 다시 시작한다. VS Code 미리보기는 (1)을 글자로 보여주니 판정 기준으로 쓰지 말 것. -->

{: .time }
**60분 타이머.** [흐름](./glossary.html#term-flow)(단건 갱신)을 먼저 만들고, [토픽](./glossary.html#term-topic)(게이트·카드·[HITL](./glossary.html#abbr-hitl))을 짓습니다.

---

## 이번 랩은

**왜** — 전형단계를 바꾸는 건 **되돌리기 어려운 결정**입니다. 그래서 바꾸기 직전에 **사람에게 한 번 묻습니다.** 그 "묻고 답 받는" 자리가 토픽입니다.

**어디쯤** — 파이프라인의 **끝**이자, 오늘의 두 HITL 패턴 중 **두 번째**입니다.

```mermaid
flowchart LR
    L4["적재<br/>Lab 4"] --> L5["승인<br/>👤 비동기<br/>Lab 5"]
    L5 --> L6["적합도<br/>Lab 6"]
    L6 --> L7["전형단계 변경<br/>👤 대화형<br/>Lab 7 ★"]
    style L7 fill:#ffe9a8,stroke:#d9a900,stroke-width:2px
```

**앞 랩과의 연결** — Lab 6의 흐름은 이름만 받아 **혼자 다 처리**했습니다. 이번엔 혼자로 안 됩니다 — 무엇으로 바꿀지를 **대화로 정해야** 하기 때문입니다. 그 차이가 아래 「왜 토픽인가」입니다.

{: .important }
**이 랩은 두 개를 만듭니다.** 먼저 **흐름**(1~12번)을 만들고, 그 다음 **토픽**(13~66번)을 만듭니다. 순서를 바꾸면 토픽에서 흐름을 선택할 수 없습니다. 지금 어디쯤인지는 각 `###` 소제목으로 확인하세요.

### 이번 랩 새 용어

| 용어 | 화면에서 | 한 줄 |
|---|---|---|
| [토픽](./glossary.html#term-topic) | 상단 메뉴 **토픽** | 특정 요청을 처리하는 **대화 시나리오**. 노드를 순서대로 실행한다 |
| 노드 | 토픽 편집기의 상자 하나 | 질문·조건·메시지·흐름 호출 등 한 동작 단위 |
| Power Fx | 칸의 **수식(fx)** 모드 | Copilot Studio의 수식 언어. 흐름 쪽 식(`concat`)과 **문법이 다르다** (`&`로 잇고 `If()`를 쓴다) |
| 레코드 | 변수 형식 선택 | 지원자 한 명의 **모든 컬럼이 담긴 한 덩어리** |
| 적응형 카드 | 대화창에 뜨는 카드 | 버튼·입력이 있는 대화형 카드. Lab 5의 승인 카드와 달리 **우리가 직접 디자인**한다 |
| slot filling | (동작) | 오케스트레이터가 대화에서 값을 미리 알아내 **질문을 건너뛰는** 것 |
| 현재 / 모든 토픽 종료 | 노드 추가 메뉴 | **현재**는 이 토픽만 끝, **모든**은 대화 전체를 끝. 확정 후엔 반드시 **모든** |

---

## 왜 토픽인가

Lab 6의 AI적합도 흐름은 이름만 받아 조회·검증·갱신을 **자급자족**했습니다. 전형단계 변경은 왜 토픽이 필요할까요?

| | Lab 6 AI적합도 흐름 | Lab 7 전형단계 토픽 |
|---|---|---|
| 입력 | 이름 + 적합도 값 | 이름만 (전형단계는 대화로 결정) |
| 확인 단계 | 없음 (결정적 실행) | **카드로 사람 확인 필수** |
| 전형단계 선택 | 해당 없음 | 챗봇과 대화로 결정 |

{: .note }
**3층 분담** — 오케스트레이터(조회·되묻기·AI적합도 전달) / **토픽**(게이트·카드·HITL) / 흐름(ID 단건 갱신).

---

## 흐름 빌드 — 전형단계변경

토픽에서 흐름을 연결하려면 흐름이 먼저 존재해야 합니다. 흐름을 먼저 만듭니다.

1. Copilot Studio에서 **면접관 에이전트**를 엽니다.

2. 왼쪽 사이드바 **흐름** → **+ 새 에이전트 흐름**을 클릭합니다. 트리거 **에이전트가 흐름을 호출할 때**가 표시되는지 확인합니다.

    ![새 에이전트 흐름 — 에이전트가 흐름을 호출할 때 트리거 확인](../assets/lab7/02.png)



3. 트리거 노드 안 **+ 입력 추가**를 클릭합니다. 입력변수를 아래와 같이 입력합니다.

    ![트리거 노드 — + 입력 추가 클릭](../assets/lab7/03.png)

4. **숫자**를 선택하고 이름 칸에 `ID`, 설명 칸에 `지원자의 ID.`를 입력합니다.

    ![입력 추가 — 숫자 선택](../assets/lab7/04.png)

5. **텍스트**를 선택하고 이름: `전형단계`, 설명: `변경할 전형단계 값(검토중/서류합격/불합격).`을 입력합니다.

    ![입력 추가 — 전형단계 텍스트 입력](../assets/lab7/05.png)

    트리거 입력 변수 2개가 완성됩니다.

    ![트리거 ID·전형단계 입력 완성](../assets/lab7/05b.png)

6. **작업 추가 → SharePoint → 항목 업데이트**를 선택합니다.

    ![작업 추가 — 항목 업데이트 선택](../assets/lab7/06.png)

7. **사이트 주소** 드롭다운에서 **`M365 Copilot GO+프로그램 (Teams/CJ올리브네트웍스)`** 를 고릅니다. **목록 이름** 칸에 본인 [목록](./glossary.html#term-list)을 선택합니다. (드롭다운에 없으면 `https://cjworld.sharepoint.com/teams/M365CopilotGOTeamsCJ` 직접 입력)


    ![항목 업데이트 — 사이트 주소·목록 이름 설정](../assets/lab7/07.png)

8. **ID** 칸에 트리거의 **ID** 칩을 넣습니다.

    ![항목 업데이트 — ID 칸에 트리거 ID 칩 매핑](../assets/lab7/08.png)
9. **ReviewStatus Value** 칸에 트리거의 **전형단계** 칩을 넣습니다.

    ![ReviewStatus Value — 전형단계 칩 매핑](../assets/lab7/09.png)

    매핑이 완성됩니다.

    ![항목 업데이트 — ID·ReviewStatus 매핑 완성](../assets/lab7/09b.png)

    {: .note }
    ID를 직접 받아 **1건만 정확히 갱신**합니다. 조회·동명이인 확인은 토픽이 이미 완료했기 때문에 흐름은 단건 갱신만 합니다.

10. 항목 업데이트 아래 **에이전트에게 응답** 노드를 선택합니다.

    ![에이전트에게 응답 노드 선택](../assets/lab7/10.png)

11. **+ 출력 추가** → **텍스트**. 이름: `결과`, 값: `전형단계가 변경됐습니다.`를 입력합니다.

    ![+ 출력 추가 — 텍스트 결과 입력](../assets/lab7/11.png)

    출력 설정이 완성됩니다.

    ![에이전트에게 응답 — 결과 출력 완성](../assets/lab7/11b.png)

12. 상단 흐름 이름을 **전형단계변경**으로 변경하고 **저장 → 게시**합니다.

    ![흐름 이름 전형단계변경 — 저장·게시](../assets/lab7/12.png)

---

## 토픽 빌드 — 전형단계변경

{: .note }
토픽의 내부 노드 구조는 다소 복잡합니다. 아래 순서도를 보고 어떤 흐름인지 개요를 파악하세요.

![전형단계변경 토픽 흐름](../assets/lab7/topic_flow.svg)

### 토픽 생성 + 입력 변수

{: start="13" }
13. 상단메뉴 **토픽** → **+ 새 토픽 → 빈 항목**을 클릭합니다.

    ![새 토픽 빈 항목 클릭](../assets/lab7/13.png)

14. 토픽 이름을 **전형단계변경**으로 입력합니다. **이 에이전트 선택** 노드 → **토픽이 수행하느 작업설명**을 클릭해 트리거 설명을 입력합니다

    ```
    사용자가 특정 지원자의 전형단계(검토중/서류합격/불합격) 변경을 요청할 때 호출합니다.
    ```

    ![토픽 이름 전형단계변경 + 트리거 설명 입력](../assets/lab7/14.png)

15. 상단 툴바 **세부 정보**를 클릭합니다.

    ![세부 정보 탭 클릭](../assets/lab7/15.png)

16. **입력** 탭을 선택합니다. **+ 새 변수 만들기**를 클릭합니다.

    ![입력 탭 — + 새 변수 만들기 클릭](../assets/lab7/16.png)

17. 이름: `AI적합도`, 형식: **텍스트**, 설명: `오케스트레이터가 전달하는 AI적합도 값.`을 입력합니다. 이후 **+ 새 변수 만들기**를 다시 클릭합니다

    ![AI적합도 변수 완성](../assets/lab7/17.png)

18. 이름: `ID`, 형식: **숫자**, 설명: `동명이인 해소 시 오케스트레이터가 전달하는 SP 항목 ID.`를 입력합니다.

    ![ID 변수 — 이름·숫자형·설명 입력 완성](../assets/lab7/18.png)

    {: .note }
    Question 노드 없이 오케스트레이터가 직접 채워주는 값은 여기서 선언합니다. Question 노드를 통해 사용자에게 묻는 변수(지원자이름·전형단계)는 뒤에서 추가합니다.

---

### 질문 — 지원자이름

{: start="19" }
19. **+ 추가 → 질문**을 선택합니다.

    ![+ 추가 → 질문 선택](../assets/lab7/19.png)

20. 질문 텍스트에 `어느 지원자인가요?`를 입력합니다.

    ![질문 텍스트 입력](../assets/lab7/20.png)

21. **식별** → **사용자의 전체 응답**을 선택합니다.

    ![식별 — 사용자의 전체 응답 선택](../assets/lab7/21.png)

22. **다음 이름으로 사용자 응답 저장** 변수 옆 **(x)**를 클릭해 변수 이름을 **지원자이름**으로 변경합니다. 변수 속성 패널에서 **다른 토픽에서 값 받기**를 체크합니다.

    ![지원자이름 변수 + 다른 토픽에서 값 받기 체크](../assets/lab7/22.png)

---

### 항목 가져오기 (GetItems)

{: start="23" }
23. **+ 추가 → 작업 추가 → 커넥터에서 호출 → SharePoint → 항목 가져오기**를 선택합니다.

    ![SharePoint — 항목 가져오기 선택](../assets/lab7/23.png)

    항목 가져오기 노드가 추가됩니다.

    ![항목 가져오기 노드 추가 완성](../assets/lab7/23b.png)

24. **개시** 메뉴에서 제작자의 자격증명을 설정하고, **입력** 메뉴에서 **사이트 주소**에 **`M365 Copilot GO+프로그램 (Teams/CJ올리브네트웍스)`** 를 지정합니다. **목록 이름** 칸에 본인 목록을 선택합니다. (사이트가 안 보이면 `https://cjworld.sharepoint.com/teams/M365CopilotGOTeamsCJ` 직접 입력)


    ![개시 제작자 자격증명 + 사이트·목록 설정](../assets/lab7/24.png)

    사이트·목록 설정이 완성됩니다.

    ![사이트 주소·목록 이름 설정 완성](../assets/lab7/24b.png)

25. **필터 쿼리** 칸 오른쪽 **수식(fx)** 버튼을 클릭해 수식 모드로 전환후 아래 수식을 입력합니다.

    ```powerfx
    If(IsBlank(Topic.ID), "ApplicantName eq '" & Topic.지원자이름 & "'", "ID eq " & Text(Topic.ID))
    ```

    ![필터 쿼리 — 수식 모드 입력](../assets/lab7/25.png)

26. **최대 가져올 항목 수**에 `2`를 입력합니다.

    ![최대 가져올 항목 수 2 입력](../assets/lab7/26.png)

    {: .note }
    ID가 있으면 ID로 1건, 없으면 이름으로 조회합니다. 최대 2로 동명이인 여부를 감지합니다.

---

### 변수 설정 — 행 갯수

{: start="27" }
27. **+ 추가 → 변수 값 설정**을 선택합니다.

    ![+ 추가 → 변수 값 설정 선택](../assets/lab7/27.png)

28. **변수 설정** 칸 → **+ 새 변수 만들기** → 이름: `Rownumber`, **받는 사람 값** 칸을 수식 모드로 전환 후 아래를 붙여넣습니다.

    ```powerfx
    CountRows(Topic.GetItems.value)
    ```

    {: .note }
    **이 식이 하는 일** — 앞 단계 조회가 **몇 건** 나왔는지 세어 `Rownumber`에 담습니다. `Topic.GetItems`는 조회 노드의 결과, `.value`가 그 안의 행 목록입니다. 바로 다음 조건이 이 숫자로 **0건(없음) / 2건 이상(동명이인) / 1건(정상)** 을 가릅니다.

    ![변수 설정 — Rownumber CountRows 수식 입력](../assets/lab7/28.png)

    Rownumber 변수 설정이 완성됩니다.

    ![Rownumber 변수 설정 완성](../assets/lab7/28b.png)

---

### 조건 — 행 갯수 분기

{: start="29" }
29. **+ 추가 → 조건**을 선택합니다.

    ![+ 추가 → 조건 선택](../assets/lab7/29.png)

30. 왼쪽 칸에 `Topic.Rownumber`를 선택합니다.

    ![왼쪽 칸 — Topic.Rownumber 선택](../assets/lab7/30.png)

31. 연산자: **같음**, 오른쪽 값: `0`.

    ![연산자 같음, 오른쪽 값 0 설정](../assets/lab7/31.png)

32. **True** 분기 안에 **메시지 보내기**를 추가합니다. 텍스트: `해당 이름의 지원자를 찾을 수 없습니다.`

    ![True 분기 — 메시지 보내기 추가](../assets/lab7/32.png)

    True 분기 구성이 완성됩니다.

    ![count=0 True 분기 완성](../assets/lab7/32b.png)

33. **True** 분기 안에 **현재 토픽 종료**를 추가합니다.

    ![True 분기 — 현재 토픽 종료 추가](../assets/lab7/33.png)

34. 조건 노드에서 **+ 새 조건 추가**를 클릭합니다.

    ![+ 새 조건 추가 클릭](../assets/lab7/34.png)

35. 새 분기 조건: `Topic.Rownumber` **보다 큼** `1`.

    ![새 분기 조건 — Rownumber 보다 큼 1 설정](../assets/lab7/35.png)

36. 새 분기 안에 **메시지 보내기**: `동명이인이 있습니다. 이메일이나 ID로 특정해 주세요.`. 그리고 아래에 현재 토픽 종료를 추가합니다.

    ![동명이인 분기 — 메시지 보내기 입력](../assets/lab7/36.png)

    동명이인 분기가 완성됩니다.

    ![동명이인 분기 완성](../assets/lab7/36b.png)

    {: .note }
    나머지 분기(count=1)는 추가 액션 없이 아래 노드로 이어집니다.

---

### 변수 설정 — 단일 레코드

{: start="37" }
37. **+ 추가 → 변수 값 설정**을 선택합니다.

    ![변수 값 설정 노드 추가](../assets/lab7/37.png)

38. **변수 설정** 칸 → **+ 새 변수 만들기** → 이름: `지원자Record`, 형식: **레코드**. **받는 사람 값** 칸을 수식 모드로 전환 후 아래를 붙여넣습니다.

    ![지원자Record 변수 — 이름·레코드 형식 입력](../assets/lab7/38.png)

    ```powerfx
    First(Topic.GetItems.value)
    ```

    {: .note }
    **이 식이 하는 일** — 조회 결과에서 **첫 번째 한 건**을 통째로 꺼내 `지원자Record`에 담습니다. 여기까지 왔다면 앞 조건이 "정확히 1건"을 이미 보장했으므로 첫 번째 = 그 사람입니다. 이제 `Topic.지원자Record.ID`처럼 **컬럼을 꺼내 쓸 수 있습니다.**

    ![받는 사람 값 — First() 수식 완성](../assets/lab7/38b.png)

---

### 조건 — AI적합도 게이트

{: start="39" }
39. **+ 추가 → 조건**을 선택합니다.

    ![+ 추가 → 조건 선택](../assets/lab7/39.png)

40. 왼쪽 칸: `Topic.AI적합도`, 연산자: **같음**, 오른쪽: `미적용`.

    ![조건 왼쪽 칸 — Topic.AI적합도 선택](../assets/lab7/40.png)

41. 조건 설정이 완성된 모습입니다.

    ![조건 — AI적합도 같음 미적용 완성](../assets/lab7/41.png)

42. **True** 분기 → **메시지 보내기**: `AI 적합도 평가가 완료되지 않은 지원자입니다. 평가 후 다시 요청하세요.`

    ![True 분기 — 메시지 보내기 입력](../assets/lab7/42.png)

    True 분기 메시지가 완성됩니다.

    ![AI적합도 True 분기 완성](../assets/lab7/42b.png)

43. **True** 분기 → **현재 토픽 종료**.

    ![AI적합도 게이트 — 토픽 종료 추가](../assets/lab7/43.png)

    {: .note }
    AI적합도 평가와 반영은 오케스트레이터의 몫입니다. 이 토픽은 "미적용이면 차단"만 합니다.

---

### 조건 — 승인됨 게이트

{: start="44" }
44. **+ 추가 → 조건**을 선택합니다.

45. 왼쪽 칸: `Topic.지원자Record.ApproveStatus.Value`, 연산자: **같지 않음**, 오른쪽: `승인됨`.

    ![조건 — ApproveStatus.Value 같지 않음 승인됨 설정](../assets/lab7/45.png)

    조건 설정이 완성됩니다.

    ![ApproveStatus 조건 완성](../assets/lab7/45b.png)

46. **True** 분기 → **메시지 보내기**: `AI 요약 승인이 완료되지 않은 지원자입니다.`

    ![True 분기 — 메시지 보내기](../assets/lab7/46.png)

47. **True** 분기 → **토픽 종료**.

    ![ApproveStatus 게이트 완성](../assets/lab7/47.png)

    {: .important }
    **파이프라인 불변식** — 요약 승인 → [전형단계](./glossary.html#term-review-status) 변경 순서를 토픽이 결정적으로 강제합니다.

---

### 질문 — 전형단계

{: start="48" }
48. **+ 추가 → 질문**을 선택합니다.

    ![+ 추가 → 질문 선택](../assets/lab7/48.png)

49. 질문 텍스트: `어떤 전형단계로 변경할까요?`

    ![질문 텍스트 입력](../assets/lab7/49.png)

50. **식별** → **다중 선택 옵션**을 선택합니다. **+ 새 옵션**을 클릭해 `검토중`, `서류합격`, `불합격`을 추가합니다.

    ![식별 — 다중 선택 옵션 선택](../assets/lab7/50.png)

    3개 옵션이 완성됩니다.

    ![3옵션 완성 — 검토중·서류합격·불합격](../assets/lab7/50b.png)

    {: .note }
    다중 선택 옵션의 선택은 이후 자동으로 조건이 붙습니다. 현재는 불필요하기 때문에 자동으로 생성된 조건은 삭제합니다.

51. 변수 이름을 **전형단계**로 변경하고 다른 토픽에서 값 수신을 체크합니다.

    ![변수 이름 전형단계 + 다른 토픽에서 값 수신 체크](../assets/lab7/51.png)

    {: .warning }
    옵션 값은 SP 목록 Choice 값과 **정확히** 일치해야 합니다. 오탈자가 있으면 변경 시 null이 저장됩니다.

    {: .note }
    **slot filling** — 질문 노드는 오케스트레이터가 그 값을 이미 알고 있는지에 따라 물어볼 수도, 건너뛸 수도 있습니다. 오케스트레이터가 대화에서 값을 알아내 미리 채워 주는 것을 slot filling이라고 합니다. 값이 채워지지 않은 채 토픽이 실행되면 그때 질문지가 표시됩니다.

---

### 적응형 카드 질문

{: start="52" }
52. **+ 추가 → 적응형 카드 질문**을 선택합니다.

    ![+ 추가 → 적응형 카드 질문 선택](../assets/lab7/52.png)

53. 카드 편집 창에서 **수식(fx)** 모드로 전환합니다.

    ![카드 편집 창 — 수식 모드 전환](../assets/lab7/53.png)

54. 아래 수식을 붙여넣습니다.

    ```
    {
      type: "AdaptiveCard",
      '$schema': "https://adaptivecards.io/schemas/adaptive-card.json",
      version: "1.5",
      body: [
        { type: "TextBlock", text: "전형단계 변경 확인", weight: "Bolder", size: "Medium", wrap: true },
        { type: "TextBlock", text: "지원자: " & Topic.지원자이름, wrap: true, spacing: "Medium" },
        { type: "TextBlock", text: "현재 전형단계: " & Topic.지원자Record.ReviewStatus.Value, wrap: true },
        { type: "TextBlock", text: "변경 후: " & Topic.전형단계, wrap: true, weight: "Bolder" },
        { type: "TextBlock", text: "위 내용으로 변경할까요?", wrap: true, spacing: "Large" }
      ],
      actions: [
        { type: "Action.Submit", title: "확정", style: "positive",
          data: { actionSubmitId: "review_confirm_v1", decision: "confirm" } },
        { type: "Action.Submit", title: "취소", style: "destructive",
          data: { actionSubmitId: "review_confirm_v1", decision: "cancel" } }
      ]
    }
    ```

    ![적응형 카드 수식 붙여넣기 완성](../assets/lab7/54.png)


    {: .note }
    출력이 보이지 않는 다면. `스키마 편집`의 값을 아래로 덮어 씁니다.

    ```
    kind: Record
    properties:
        actionSubmitId: String
        decision: String
    ```

    {: .note }
    적응형 카드의 기본 디자인 편집은 json모드로 수행할 수 있습니다. 다만 이때는 레이아웃 편집만 가능하고 변수를 지정하거나 가변적인 수식의 값은 정의가 되지 않습니다. 수식 카드 일때 이 작업이 가능하지만 다시 json모드로 복귀가 되지 않습니다. 레이아웃의 고민이 필요하면 json모드로 먼저 배치를 해보고 수식 모드로 확정하시는걸 추천합니다.

    {: .warning }
    카드 body에 **TextBlock과 FactSet을 섞으면** Power Fx가 스키마를 병합해 `"facts": null` 오류가 발생합니다. TextBlock 단일 타입만 사용하세요.

55. **다음 이름으로 사용자 응답 저장** 변수 이름을 **decision**으로 설정합니다.

    ![decision 변수 설정 완성](../assets/lab7/55.png)

---

### 조건 — 확정 / 취소

{: start="56" }
56. **+ 추가 → 조건**을 선택합니다. 왼쪽 칸: `Topic.decision`, 연산자: **같음**, 오른쪽: `confirm`.

    ![조건 — Topic.decision 같음 confirm 설정](../assets/lab7/56.png)

    조건 노드가 완성됩니다.

    ![조건 decision confirm 완성](../assets/lab7/56b.png)

---

### 확정 분기

{: start="57" }
57. **True** 분기 → **+ 추가 → 작업 추가**에서 **전형단계변경** 흐름을 선택합니다.

    ![True 분기 — 전형단계변경 흐름 선택](../assets/lab7/57.png)

58. 입력 **ID** 칸에 `Topic.지원자Record.ID`를 수식으로 입력합니다.

    ![흐름 입력 — ID 칸 수식 입력](../assets/lab7/58.png)

59. 입력 **전형단계** 칸을 수식 모드로 전환해 아래를 입력합니다. 흐름의 전형단계 입력은 **텍스트**를 받으므로, 선택 값을 `Text()`로 감싸 문자열로 넘깁니다.
    ```
    Text(Topic.전형단계)
    ```
    ![흐름 호출 — ID·전형단계 매핑 완성](../assets/lab7/59.png)

60. **True** 분기 → **메시지 보내기**. **False** 분기 → **메시지 보내기**: `변경을 취소했습니다.`

    ```
    {Topic.지원자이름}의 전형단계를 {Text(Topic.전형단계)}(으)로 변경했습니다.
    ```

    ![확정·취소 분기 — 메시지 보내기 완성](../assets/lab7/60.png)

61. 최하단 노드에 **모든 토픽 종료** 를 추가합니다.

    **모든 토픽 종료**가 없으면 토픽이 자신을 다시 부르는 **재귀 호출**이 발생합니다. 확정·취소 **양쪽 모두** 반드시 넣으세요.

    ![모든 토픽 종료 — 양쪽 분기 완성](../assets/lab7/61.png)

    {: .important }
    **✅ 여기까지 점검 — 토픽 완성** — 위에서부터 훑어 내려가며 확인하세요. **질문**(지원자이름) → **조회** → **행 갯수 분기**(0건·2건 이상 차단) → **AI적합도 게이트** → **승인됨 게이트** → **질문**(전형단계) → **확인 카드** → **확정/취소 분기**. 차단 분기마다 **메시지 + 토픽 종료**가 짝으로 있어야 하고, 맨 끝 두 갈래에는 **모든 토픽 종료**가 있어야 합니다. 하나라도 비면 그 경우에 대화가 멈추거나 무한히 돕니다.

62. **저장** 후 [지침](./glossary.html#term-instructions)을 추가합니다.

    ```
    ## 전형단계 변경 규칙
    - 전형단계 변경 요청 시 전형단계변경 토픽을 호출한다.
    - 변경 가능한 전형단계 값: 검토중, 서류합격, 불합격
    - 이 외의 값(최종합격, 면접합격 등)은 존재하지 않으므로 "지원되지 않는 전형단계입니다"라고 안내하고 토픽을 호출하지 않는다.
    - 여러 명·전원 일괄 변경은 거절하고 한 명씩 지정하게 한다.
    - 동명이인이 있으면 이메일 또는 ID로 특정하도록 먼저 되묻는다.
    ```

    {: .important }
    **이 지침이 토픽 앞을 지킵니다.** 토픽 안의 게이트는 "들어온 요청"을 검사하지만, 이 지침은 **애초에 토픽을 부를지 말지**를 정합니다. 없는 전형단계(면접합격 등)나 일괄 변경 요청은 **토픽에 닿기 전에** 여기서 걸러집니다 — 63~66번 테스트가 정확히 이 두 층을 확인합니다.

    ![오케스트레이터 지침 — 전형단계 변경 규칙 섹션 추가](../assets/lab7/62.png)



---

## 테스트 — 4가지 시나리오

네 개를 **모두** 돌려 보세요. 정상 동작 하나만 확인하면 게이트가 실제로 막는지 알 수 없습니다. **막히는 것을 확인하는 것**이 이 테스트의 목적입니다.

{: start="63" }
63. **정상** — 승인된 지원자에게 `○○○ 서류합격 처리해줘`

    - 기대: 확인 카드가 뜨고 → **[확정]** → `○○○의 전형단계를 서류합격(으)로 변경했습니다.`
    - 목록에서 그 지원자의 **전형단계가 실제로 바뀌었는지** 확인합니다. 메시지만 보고 넘어가지 마세요.

64. **없는 이름** — 존재하지 않는 이름으로 변경 요청

    - 기대: `해당 이름의 지원자를 찾을 수 없습니다.` → 대화 종료
    - 막은 곳: 토픽의 **행 갯수 = 0** 분기 (29~33번)

65. **미승인 지원자** — 요약이 아직 승인되지 않은 지원자로 변경 요청

    - 기대: 승인이 필요하다는 안내 → 대화 종료. **목록 값은 그대로**여야 합니다
    - 막은 곳: 토픽의 **승인됨 게이트** (44~47번)

66. **잘못된 전형단계** — `면접합격으로 바꿔줘`

    - 기대: `지원되지 않는 전형단계입니다` 안내. **토픽이 아예 호출되지 않습니다**
    - 막은 곳: **오케스트레이터 지침** (62번) — 토픽 바깥, 대화 단계에서 차단

{: .note }
64·65는 **토픽 안쪽**에서, 66은 **토픽 바깥**에서 막힙니다. 같은 "차단"이지만 층이 다릅니다 — 지침은 대화를 지키고, 게이트는 데이터를 지킵니다.

---

## 확인

- [ ] **세부 정보→입력**에 AI적합도·ID가 선언됐다
- [ ] GetItems 필터가 ID 있으면 ID 조회, 없으면 이름 조회로 분기한다
- [ ] 게이트 ① count=1, 게이트 ② ApproveStatus=승인됨 — 둘 다 있다
- [ ] AI적합도 미적용이면 토픽에서 차단한다
- [ ] 카드 body가 TextBlock 단일 타입이다 (FactSet 없음)
- [ ] 확정·취소 **양쪽** 끝에 **모든 토픽 종료**가 있다
- [ ] 흐름 입력이 **ID(숫자)·전형단계(텍스트)** 두 개다

{: .important }
**읽기는 커넥터+지침, 상태 변경은 흐름, 사람 확인은 토픽.** 같은 "사람 끼워 넣기"도 비동기(Lab 5)와 대화형(지금)으로, 같은 확인도 텍스트와 카드로 갈립니다. 그 경계를 손으로 그어 본 것 — 이 과정이 남기는 핵심입니다.
